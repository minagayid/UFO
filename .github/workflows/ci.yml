name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  security-events: write
  checks: write

jobs:
  security:
    name: Security Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run npm audit
        run: |
          npm ci
          npm audit --audit-level=moderate || npm audit fix --force
        continue-on-error: true

      - name: Check for sensitive data
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD

  build:
    name: Build Application
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: package.json

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.node-version }}
          path: dist
          retention-days: 5

  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package.json

      - name: Install dependencies
        run: npm ci

      - name: Check for console logs
        run: |
          if grep -r "console\." src/ --include="*.tsx" --include="*.ts" | grep -v "// console\." | grep -v "console\.error"; then
            echo "Warning: console statements found in source code"
          fi

      - name: Check for TODO/FIXME comments
        run: |
          echo "=== TODOs in source code ==="
          grep -r "TODO\|FIXME" src/ --include="*.tsx" --include="*.ts" || echo "None found"

  dependencies:
    name: Dependency Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package.json

      - name: Check outdated dependencies
        run: |
          npm ci
          npm outdated || true

  readiness:
    name: Production Readiness
    runs-on: ubuntu-latest
    needs: [build, lint, security]
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - name: Check README
        run: |
          if [ ! -f README.md ] || [ ! -s README.md ]; then
            echo "❌ README.md is missing or empty"
            exit 1
          fi
          if ! grep -q "Installation\|Setup\|Usage" README.md; then
            echo "⚠️ README.md lacks key sections"
          fi

      - name: Check LICENSE
        run: |
          if [ ! -f LICENSE ]; then
            echo "⚠️ No LICENSE file found"
          fi

      - name: Check CONTRIBUTING guidelines
        run: |
          if [ ! -f CONTRIBUTING.md ]; then
            echo "ℹ️ Consider adding CONTRIBUTING.md"
          fi

      - name: Summary
        run: |
          echo "✅ All CI checks passed!"
          echo "Ready for deployment"